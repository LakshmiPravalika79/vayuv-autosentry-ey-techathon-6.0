# ===========================================
# AutoSentry AI - Environment Configuration
# ===========================================
# ⚠️ WARNING: Never commit this file with real credentials!
# Copy this file to .env and fill in your values

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Options: postgres | firebase
DATABASE_MODE=postgres

# PostgreSQL Configuration (used when DATABASE_MODE=postgres)
DATABASE_URL=postgres://postgres:postgres@postgres:5432/autosentry

# Firebase Configuration (used when DATABASE_MODE=firebase)
# 1. Create a Firebase project at https://console.firebase.google.com
# 2. Go to Project Settings > Service Accounts
# 3. Generate a new private key (JSON file)
# 4. Save the JSON file and update the path below
FIREBASE_PROJECT_ID=
FIREBASE_CREDENTIALS_PATH=./credentials/firebase-service-account.json

# ===========================================
# REDIS CONFIGURATION
# ===========================================
REDIS_URL=redis://redis:6379

# ===========================================
# MINIO (S3-COMPATIBLE STORAGE)
# ===========================================
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=MINIOACCESSKEY
MINIO_SECRET_KEY=MINIOSECRETKEY
MINIO_BUCKET_NAME=autosentry

# ===========================================
# AUTHENTICATION
# ===========================================
# Generate a secure random string for production
JWT_SECRET=supersecretjwt_change_in_production
JWT_EXPIRES_IN=24h

# ===========================================
# LLM CONFIGURATION (Optional)
# ===========================================
# Enable LLM for enhanced agent responses
LLM_ENABLED=false

# LLM Provider: openai | anthropic
LLM_PROVIDER=openai

# API Keys - Get from:
# OpenAI: https://platform.openai.com/api-keys
# Anthropic: https://console.anthropic.com/
LLM_API_KEY=REPLACE_WITH_YOUR_KEY

# Model selection
LLM_MODEL=gpt-4-turbo-preview

# ===========================================
# AGENT FRAMEWORK
# ===========================================
# Options: builtin | langgraph | crewai | autogen
AGENT_FRAMEWORK=builtin

# LangGraph specific settings
LANGGRAPH_CHECKPOINT_DIR=./checkpoints

# CrewAI specific settings
CREWAI_VERBOSE=true

# AutoGen specific settings
AUTOGEN_CONFIG_PATH=./agents/autogen_config.json

# ===========================================
# SERVICE URLS (for local development)
# ===========================================
ML_SERVICE_URL=http://ml-service:5000
UEBA_SERVICE_URL=http://ueba-service:5001
BACKEND_URL=http://backend:4000
FRONTEND_URL=http://localhost:3000

# ===========================================
# TELEMETRY SIMULATOR
# ===========================================
TELEMETRY_INTERVAL_MS=5000
TELEMETRY_VEHICLES_COUNT=10

# ===========================================
# LOGGING
# ===========================================
LOG_LEVEL=info
LOG_FORMAT=json

# ===========================================
# DEVELOPMENT
# ===========================================
NODE_ENV=development
DEBUG=false
